# 🎯 Интеграция с внешними источниками данных

## 1. TikTok Creative Center API (если доступен)

```python
# utils/external_data.py

import requests
from typing import List, Dict

class TikTokCreativeCenter:
    """
    Получение публичных трендовых креативов.
    НЕ ДАЕТ CVR! Только engagement metrics.
    """

    def __init__(self, api_key: str = None):
        self.api_key = api_key
        self.base_url = "https://ads.tiktok.com/creative_radar_api/v1"

    def get_trending_videos(
        self,
        industry: str = "gaming",
        country: str = "US",
        limit: int = 20
    ) -> List[Dict]:
        """
        Получить трендовые видео.

        ВАЖНО: Данные только по engagement, НЕ по конверсиям!
        """

        # NOTE: TikTok Creative Center может не иметь публичного API
        # Это пример структуры, если API появится

        endpoint = f"{self.base_url}/popular_trend/list"

        params = {
            "industry_id": industry,
            "country_code": country,
            "limit": limit
        }

        # Пока недоступно
        print("⚠️ TikTok Creative Center API недоступен")
        print("   Используйте ручной анализ через https://ads.tiktok.com/business/creativecenter")

        return []


## 2. Spy Tools Integration (если есть API)

class PipiadsAPI:
    """
    Интеграция с Pipiads (платный сервис).
    Дает креативы конкурентов, НЕ точные CVR.
    """

    def __init__(self, api_key: str):
        self.api_key = api_key
        self.base_url = "https://api.pipiads.com"

    def search_ads(
        self,
        keyword: str = "lootbox",
        platform: str = "tiktok",
        min_days_running: int = 30
    ) -> List[Dict]:
        """
        Найти креативы конкурентов.

        min_days_running: Если креатив льется 30+ дней,
                         скорее всего он profitable
        """

        # Пример (если API доступен)
        print("⚠️ Требуется подписка Pipiads ($99/мес)")
        print("   https://www.pipiads.com/")

        return []


## 3. Benchmark данные (статичные)

INDUSTRY_BENCHMARKS = {
    "lootbox": {
        "avg_cvr": 0.10,      # 10%
        "top_10_cvr": 0.15,   # 15%
        "avg_ctr": 0.03,      # 3%
        "source": "Industry estimates 2024"
    },
    "sports_betting": {
        "avg_cvr": 0.08,
        "top_10_cvr": 0.12,
        "avg_ctr": 0.025,
        "source": "Industry estimates 2024"
    },
    "casino": {
        "avg_cvr": 0.06,
        "top_10_cvr": 0.10,
        "avg_ctr": 0.02,
        "source": "Industry estimates 2024"
    }
}

def get_industry_benchmark(product_category: str) -> Dict:
    """
    Получить benchmark для холодного старта.

    ВАЖНО: Это усредненные данные, не для конкретных паттернов!
    Используйте только для первичной оценки.
    """

    return INDUSTRY_BENCHMARKS.get(product_category, {
        "avg_cvr": 0.05,
        "top_10_cvr": 0.08,
        "avg_ctr": 0.02,
        "source": "Generic estimate"
    })


## 4. Холодный старт с предположениями

def cold_start_prediction(
    hook_type: str,
    emotion: str,
    industry_benchmark: float = 0.10
) -> Dict:
    """
    Предсказание для холодного старта (без данных).

    ВАЖНО: Низкая точность! Используйте только до первого теста.
    """

    # Эвристика (на основе общих наблюдений)
    hook_multipliers = {
        "wait": 1.2,      # Обычно +20% от среднего
        "question": 1.1,
        "bold_claim": 0.9,
        "urgency": 1.3,
        "curiosity": 1.15
    }

    emotion_multipliers = {
        "excitement": 1.1,
        "fear": 1.2,
        "curiosity": 1.15,
        "greed": 1.25,
        "fomo": 1.3
    }

    multiplier = (
        hook_multipliers.get(hook_type, 1.0) *
        emotion_multipliers.get(emotion, 1.0)
    ) / 2  # Average of both

    predicted_cvr = industry_benchmark * multiplier

    return {
        "predicted_cvr": predicted_cvr,
        "confidence_score": 0.3,  # Очень низкая!
        "method": "cold_start_heuristic",
        "warning": "Low accuracy - run real test ASAP",
        "recommendation": "Collect data from 20 creatives first"
    }
```

---

## 🚨 ВАЖНЫЕ ОГРАНИЧЕНИЯ

### 1. Legal & Ethical

```
❌ Scraping TikTok без разрешения - нарушение ToS
❌ Scraping чужих рекламных данных - может быть незаконно
❌ Использование данных конкурентов без согласия - этически спорно

✅ Spy tools (платные) - легально, это их бизнес-модель
✅ Публичные API (если есть) - легально
✅ Свои данные - легально и надежно
```

### 2. Точность без своих данных

```
Benchmark данные:
  Точность: ±5-10 процентных пунктов
  Пример: Benchmark CVR = 10%, реальный CVR = 5-15%

Spy tools оценки:
  Точность: "креатив работает" или "не работает"
  Точный CVR: НЕИЗВЕСТЕН

Холодный старт эвристика:
  Точность: ±15 процентных пунктов (очень низкая!)
  Использовать только для первичной фильтрации
```

---

## 💰 Реальная стратегия запуска

### Фаза 1: Подготовка (0-2 недели, $0-99)

```
1. Spy tools (опционально) → $99/мес
   - Изучить конкурентов
   - Выявить работающие паттерны

2. Benchmark research (бесплатно)
   - Reddit r/PPC
   - Industry reports
   - Case studies

3. Планирование тестов
   - 20 креативов с разными паттернами
   - Бюджет $4,000
```

### Фаза 2: Холодный старт (2-6 недель, $4,000)

```
1. Заказ UGC: 20 видео × $150 = $3,000
2. Микро-тесты: 20 × $50 = $1,000
3. Сбор данных: 7 дней
4. Обучение модели: POST /train-markov-chain
```

### Фаза 3: Работа модели (6+ недель, экономия $5k/цикл)

```
1. Новые креативы: 10 шт × $150 = $1,500
2. Предсказание CVR через API
3. Тест только топ-3
4. Масштабирование
5. Profit! 💰
```

---

## ❌ ЧТО НЕ РАБОТАЕТ (честно)

```
"Можно ли парсить TikTok и за 24 часа получить данные?"
❌ НЕТ. TikTok API закрыт, scraping запрещен, за 24 часа не соберешь статистику.

"Можно ли использовать лайки вместо CVR?"
❌ НЕТ. Корреляция слабая. Video с 1M лайков может иметь CVR 2%.

"Можно ли обучить модель без своих данных?"
❌ НЕТ. Чужие данные закрыты. Benchmark - слишком неточен.

"Можно ли избежать холодного старта?"
❌ НЕТ. Первые $4k придется потратить на сбор данных.
```

---

## ✅ ЧТО РАБОТАЕТ (реально)

```
1. Spy tools → идеи паттернов (не CVR!)
2. Cold start $4k → собрать свои данные
3. 5 недель → обученная модель
4. Цикл 2+ → экономия 70%
5. ROI окупается за 2-3 цикла
```

**Bottom line: Нет бесплатного сыра. Холодный старт неизбежен.** 🎯
