# Admin Bot - Telegram бот для управления UTM метками

🤖 Удобная админка прямо в Telegram для генерации UTM ссылок и просмотра аналитики.

---

## Возможности

### 🔗 Генерация UTM ссылок
- Создание tracking ссылок для TikTok/Instagram/YouTube
- Автоматическое создание уникальных UTM ID
- Интерактивный процесс с кнопками
- Мгновенная копируемая ссылка

### 📊 Аналитика
- Статистика за день/неделю/месяц
- Топ источников трафика
- Детализация по кампаниям
- Последние конверсии в реальном времени

### 🔔 Уведомления
- Ежедневная статистика (в 9:00)
- Уведомления о новых покупках (опционально)
- Алерты при падении CR (опционально)

---

## Быстрый старт

### Шаг 1: Создать бота в @BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Придумайте имя: `My Admin Bot`
4. Придумайте username: `my_admin_bot`
5. Скопируйте токен: `123456:ABC-DEF...`

### Шаг 2: Получить свой Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Отправьте `/start`
3. Скопируйте ваш ID: `123456789`

### Шаг 3: Настроить .env

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Admin Bot Token
ADMIN_BOT_TOKEN=123456:ABC-DEF...  # Ваш токен из BotFather

# Tracking API URL
TRACKING_API_URL=http://localhost:8000

# Admin User IDs
ADMIN_IDS=123456789  # Ваш Telegram ID

# JWT Token (получим на следующем шаге)
ADMIN_JWT_TOKEN=
```

### Шаг 4: Получить JWT Token

Сначала запустите tracking API:

```bash
# В отдельном терминале
python api/main.py
```

Создайте админского пользователя:

```bash
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "your_secure_password"
  }'
```

Получите JWT токен:

```bash
curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "your_secure_password"
  }'
```

Скопируйте `access_token` из ответа и вставьте в `.env`:

```env
ADMIN_JWT_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### Шаг 5: Запустить бота

```bash
python admin_bot.py
```

Вы увидите:

```
🤖 Admin Bot starting...
👤 Admins: [123456789]
📊 API: http://localhost:8000
✅ Bot is running!
```

### Шаг 6: Открыть бота в Telegram

1. Найдите вашего бота: `@my_admin_bot`
2. Отправьте `/start`
3. Вы должны увидеть меню с командами!

---

## Команды бота

### /generate - Создать UTM ссылку

Интерактивный процесс:

```
Вы: /generate

Бот: 🔗 Генератор UTM ссылок
     Введите название кампании:

Вы: football_jan_2025

Бот: ✅ Кампания: football_jan_2025
     Выберите источник трафика:
     [TikTok] [Instagram] [YouTube] [Other]

Вы: [TikTok]

Бот: ✅ Источник: tiktok
     Введите content (ID видео):

Вы: video_messi_goal

Бот: ✅ UTM ссылка создана!

     🔗 Ссылка для TikTok:
     https://yourdomain.com/l/tiktok_a7b3c

     📊 Параметры:
     • Campaign: football_jan_2025
     • Source: tiktok
     • Content: video_messi_goal
     • UTM ID: tiktok_a7b3c_8f2e1

     [📊 Статистика] [🔄 Создать еще]
```

### /stats - Статистика

```
Вы: /stats

Бот: 📊 Статистика
     Выберите период:
     [Сегодня] [7 дней] [30 дней]

Вы: [Сегодня]

Бот: 📊 Статистика сегодня

     👆 Клики: 245
     💰 Конверсии: 12 (CR: 4.9%)
     💵 Выручка: $600
     📈 AOV: $50

     🏆 Топ источники:
     1. TIKTOK - 8 конв, $400
     2. INSTAGRAM - 3 конв, $150
     3. ORGANIC - 1 конв, $50

     [🔄 Обновить] [📈 Детали]
```

### /campaigns - Список кампаний

```
Вы: /campaigns

Бот: 📁 Активные кампании:

     1. football_jan_2025
        👆 150 | 💰 8 (5.3%) | 💵 $400

     2. basketball_highlights
        👆 95 | 💰 4 (4.2%) | 💵 $200

     [📊 football_jan_2025]
     [📊 basketball_highlights]
```

### /top - Топ источников

```
Вы: /top

Бот: 🏆 Топ источников по выручке:

     🥇 TIKTOK
        👆 500 кликов | 💰 25 конв (5.0%) | 💵 $1,250

     🥈 INSTAGRAM
        👆 300 кликов | 💰 12 конв (4.0%) | 💵 $600

     🥉 YOUTUBE
        👆 200 кликов | 💰 8 конв (4.0%) | 💵 $400
```

### /conversions - Последние покупки

```
Вы: /conversions

Бот: 💰 Последние 10 конверсий:

     • $50.00 - Gold Lootbox
       👤 User: 987654321 | ⏰ 5м назад

     • $25.00 - Silver Lootbox
       👤 User: 123456789 | ⏰ 15м назад

     • $100.00 - Platinum Lootbox
       👤 User: 555555555 | ⏰ 1ч назад
```

### /settings - Настройки уведомлений

```
Вы: /settings

Бот: ⚙️ Настройки уведомлений

     🔔 Включить уведомления о:

     ✅ Новые конверсии
     ✅ Ежедневная статистика
     ❌ Алерты (падение CR)
```

---

## Автоматические уведомления

### Ежедневная статистика (9:00)

```
📊 Статистика за вчера

👆 Клики: 450
💰 Конверсии: 22 (CR: 4.9%)
💵 Выручка: $1,100
📈 AOV: $50
```

### Новая конверсия (опционально)

```
💰 Новая покупка!

Сумма: $50.00
Продукт: Gold Lootbox
Источник: TikTok → football_jan_2025
Пользователь: 123456789

[📊 Статистика кампании]
```

---

## Примеры использования

### Сценарий 1: Создание TikTok кампании

```bash
# 1. Генерируем UTM ссылку
/generate
→ football_highlights
→ TikTok
→ video_123

# 2. Получаем ссылку
https://yourdomain.com/l/tiktok_abc123

# 3. Вставляем в TikTok bio

# 4. Смотрим статистику через несколько дней
/campaigns
→ football_highlights: 150 кликов, 8 конверсий, $400
```

### Сценарий 2: Сравнение источников

```bash
# Создаём ссылки для разных источников
/generate → football_jan → TikTok
/generate → football_jan → Instagram
/generate → football_jan → YouTube

# Через неделю смотрим топ
/top
→ TikTok: $1,000 (лучший!)
→ Instagram: $600
→ YouTube: $200
```

### Сценарий 3: A/B тест креативов

```bash
# Создаём разные content варианты
/generate → football_jan → TikTok → hook_wait
/generate → football_jan → TikTok → hook_watch
/generate → football_jan → TikTok → hook_omg

# Смотрим какой hook лучше
/campaigns → football_jan → детали
```

---

## Troubleshooting

### Бот не отвечает

**Проблема:** Отправляю `/start`, но нет ответа

**Решение:**
1. Проверьте что бот запущен: `python admin_bot.py`
2. Проверьте `ADMIN_IDS` в `.env` - ваш ID должен быть там
3. Проверьте токен бота в `.env`

### Ошибка "У вас нет доступа"

**Проблема:** Бот отвечает "❌ У вас нет доступа к этому боту"

**Решение:**
1. Получите ваш Telegram ID: [@userinfobot](https://t.me/userinfobot)
2. Добавьте в `.env`: `ADMIN_IDS=ваш_id`
3. Перезапустите бота

### Ошибка "Ошибка при загрузке данных"

**Проблема:** Команды `/stats`, `/campaigns` не работают

**Решение:**
1. Проверьте что tracking API запущен: `curl http://localhost:8000/health`
2. Проверьте `TRACKING_API_URL` в `.env`
3. Проверьте `ADMIN_JWT_TOKEN` - возможно истёк (обновите через `/api/v1/auth/login`)

### JWT токен истёк

**Проблема:** Через 60 минут бот перестаёт работать

**Решение:**

Вариант 1: Увеличить срок жизни токена в `.env`:
```env
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=43200  # 30 дней
```

Вариант 2: Использовать refresh token (TODO: добавить в следующей версии)

---

## Production Deployment

### Запуск на сервере (systemd)

Создайте `/etc/systemd/system/admin-bot.service`:

```ini
[Unit]
Description=Admin Bot for UTM Tracking
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/tg-reposter
ExecStart=/home/ubuntu/tg-reposter/venv/bin/python admin_bot.py
Restart=always
RestartSec=10

Environment="PATH=/home/ubuntu/tg-reposter/venv/bin"
EnvironmentFile=/home/ubuntu/tg-reposter/.env

[Install]
WantedBy=multi-user.target
```

Запустите:

```bash
sudo systemctl daemon-reload
sudo systemctl enable admin-bot
sudo systemctl start admin-bot
sudo systemctl status admin-bot
```

### Запуск в Docker

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY admin_bot.py .
COPY .env .

CMD ["python", "admin_bot.py"]
```

```bash
docker build -t admin-bot .
docker run -d --name admin-bot --env-file .env admin-bot
```

### Мониторинг

Добавьте логирование:

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('admin_bot.log'),
        logging.StreamHandler()
    ]
)
```

Проверяйте логи:

```bash
tail -f admin_bot.log
```

---

## Roadmap

Планируемые фичи:

- [ ] Экспорт статистики в CSV/Excel
- [ ] Графики и чарты прямо в Telegram
- [ ] Webhook для мгновенных уведомлений о конверсиях
- [ ] Multi-user режим (несколько админов с разными правами)
- [ ] Автоматические рекомендации по оптимизации
- [ ] Интеграция с Google Analytics
- [ ] A/B тест landing pages
- [ ] QR коды для offline кампаний

---

## FAQ

**Q: Можно ли добавить несколько админов?**

A: Да! Добавьте их IDs через запятую:
```env
ADMIN_IDS=123456789,987654321,555555555
```

**Q: Как получить уведомление о каждой покупке?**

A: Добавьте в `admin_bot.py`:
```python
# В конце файла telegram_bot_integration.py
def notify_admins_about_conversion(amount, product, user_id):
    for admin_id in ADMIN_IDS:
        bot.send_message(
            admin_id,
            f"💰 Новая покупка!\n"
            f"Сумма: ${amount}\n"
            f"Продукт: {product}\n"
            f"User: {user_id}"
        )
```

**Q: Бот работает только когда компьютер включён?**

A: Да, для 24/7 работы разверните на сервере (см. Production Deployment выше).

---

## Готово!

Теперь у вас есть полноценная админка в Telegram для управления UTM метками!

📱 Всегда под рукой
🚀 Быстрый доступ
📊 Вся аналитика в одном месте
🔔 Автоматические уведомления

Вопросы? Откройте issue на GitHub!
