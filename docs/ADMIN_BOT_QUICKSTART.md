# Admin Bot - Быстрый старт (5 минут)

## 1. Создать бота (1 мин)

1. Открыть [@BotFather](https://t.me/BotFather)
2. `/newbot` → название → username
3. Скопировать токен

## 2. Узнать свой ID (30 сек)

1. Открыть [@userinfobot](https://t.me/userinfobot)
2. `/start`
3. Скопировать ID

## 3. Настроить .env (2 мин)

```bash
# Скопировать пример
cp .env.example .env

# Отредактировать .env
nano .env
```

Заполнить:

```env
ADMIN_BOT_TOKEN=ваш_токен_из_botfather
ADMIN_IDS=ваш_telegram_id
TRACKING_API_URL=http://localhost:8000
```

## 4. Получить JWT токен (1 мин)

```bash
# Запустить API (в отдельном терминале)
python api/main.py

# Создать пользователя
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{"email": "admin@test.com", "password": "admin123"}'

# Получить токен
curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email": "admin@test.com", "password": "admin123"}'

# Скопировать access_token и добавить в .env
# ADMIN_JWT_TOKEN=...
```

## 5. Запустить бота (30 сек)

```bash
# Установить зависимости (если еще не сделали)
pip install -r requirements.txt

# Запустить
python admin_bot.py
```

## 6. Тест (30 сек)

1. Найти бота в Telegram
2. `/start`
3. `/generate` → создать UTM ссылку
4. Готово! 🎉

---

## Команды

```
/generate    - 🔗 Создать UTM ссылку
/stats       - 📊 Статистика
/campaigns   - 📁 Кампании
/top         - 🏆 Топ источников
/conversions - 💰 Последние покупки
```

---

## Проблемы?

**Бот не отвечает:**
- Проверьте ADMIN_IDS в .env
- Убедитесь что бот запущен

**"Ошибка при загрузке":**
- Проверьте что API запущен
- Проверьте ADMIN_JWT_TOKEN

Полная документация: [ADMIN_BOT_README.md](./ADMIN_BOT_README.md)
